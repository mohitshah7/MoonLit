import React from 'react';
import './Profile.css'; // Create a CSS file for profile styles

const Profile = () => {
  return (
    <div className="profile">
      <h2>User Profile</h2>
      {/* Add profile details like user image, bio, etc. */}
    </div>
  );
};

export default Profile;
